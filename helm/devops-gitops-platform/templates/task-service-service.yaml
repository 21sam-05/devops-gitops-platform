apiVersion: v1
kind: Service

metadata:
  name: task-service

spec:
  type: {{ .Values.taskService.service.type }}

  selector:
    app: task-service

  ports:
    - port: {{ .Values.taskService.service.port }}
      targetPort: 8000